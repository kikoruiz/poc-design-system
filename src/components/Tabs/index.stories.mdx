import {
  ArgsTable,
  Canvas,
  Meta,
  Story,
  DocsContainer,
} from "@storybook/addon-docs";
import { LiveProvider, LiveEditor, LiveError, LivePreview } from "react-live";
import { CodeSnippet, ReactLiveBlock } from "@stories/ReactLiveBlock";
import { Title } from "@stories/Title";
import cx from "classnames";

import { Tabs } from ".";
import * as examples from "./TabsExamples.stories";

<Meta title="Components/Forms/Tabs" component={Tabs} />

# Tabs

A set of layered sections of content—known as tab panels—that are displayed one at a time.

<Title label="Import" />

<CodeSnippet code='import { Tabs } from "@brikke-ui/tabs";' />

<Title label="Usage" />

TODO

<Title label="Props" />

export const propsTabs = [
  {
    title: "<Tabs />",
    value: "tab1",
    component: Tabs,
  },
  {
    title: "<Tabs.List />",
    value: "tab2",
    component: Tabs.List,
  },
  {
    title: "<Tabs.Trigger />",
    value: "tab3",
    component: Tabs.Trigger,
  },
  {
    title: "<Tabs.Content />",
    value: "tab4",
    component: Tabs.Content,
  },
];

<Tabs defaultValue="tab1">
  <Tabs.List>
    {propsTabs.map(({ title, value }) => (
      <Tabs.Trigger key={`tab-trigger-${value}`} value={value}>
        {title}
      </Tabs.Trigger>
    ))}
  </Tabs.List>
  {propsTabs.map(({ value, component }) => (
    <Tabs.Content key={`tab-content-${value}`} value={value}>
      <ArgsTable of={component} />
    </Tabs.Content>
  ))}
</Tabs>

<Title label="Variants" />

<Story name="variants" story={examples.variants} />
